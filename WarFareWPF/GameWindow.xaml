<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Converters;assembly=Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="WarFareWPF.GameWindow"
        xmlns:local="clr-namespace:WarFareWPF"
        Title="GameWindow" Height="600" Width="800" x:Name="_this" DataContext="{Binding RelativeSource={RelativeSource Self}}" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" WindowState="Maximized">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <converters:RatioToPointsConverter x:Key="RatioToPointsConverter"/>
        <converters:RatioToHeightConverter x:Key="RatioToHeightConverter"/>
        <converters:RatioToMarginConverter x:Key="RatioToMarginConverter"/>
        <converters:RatioToCoordConverter x:Key="RatioToCoordConverter"/>
        <converters:PositionToVisibilityConverter x:Key="PositionToVisibilityConverter"/>
        <converters:ListToVis x:Key="ListToVis"/>
        <converters:BoolToBackgroundConverter x:Key="BoolToBackgroundConverter"/>
        
        
        <!--<converters:MultiBooleanToVisibilityConverter x:Key="MConverter" />-->
    </Window.Resources>
    <Border BorderThickness="2" BorderBrush="Black" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" IsEnabled="True" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ListBox Name="listCase" DataContext="{Binding map, ElementName=_this}" SelectedItem="{Binding SelectedBox, Mode=TwoWay}" ItemsSource="{Binding cases}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="{Binding Columns}" Margin="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToMarginConverter}, ConverterParameter=5 15 45 15}" VerticalAlignment="Center"  MouseWheel="Grid_MouseWheel"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <ContentPresenter Margin="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToMarginConverter}, ConverterParameter=0 -10 0 -10}"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Center">
                                    <!--ROW_PAIR-->
                                    <Grid Tag="{Binding Uid}" Height="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToHeightConverter}}" Visibility="{Binding RowPair, Converter={StaticResource BoolToVis}}" MouseRightButtonUp="SelectUnit">
                                        <!--Cases-->
                                        <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Stroke="Black">
                                            <Polygon.RenderTransform>
                                                <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                            </Polygon.RenderTransform>
                                            <Polygon.Fill>
                                                <ImageBrush ImageSource="{Binding Src}"/>
                                            </Polygon.Fill>
                                        </Polygon>
                                        <!--Unités-->
                                        <Grid Visibility="{Binding HasUnitJ1, Converter={StaticResource BoolToVis}}">
                                            <!--Unité Joueur 1-->
                                            <!-- Ajouter un flag si l'unité a déjà joué (HasAlreadyPlayed) -->
                                            <Rectangle Width="20" Height="20" Fill="{Binding J1.peuple.Color, ElementName=_this}"/>
                                            <!-- Si une unité est sélectionnée -->
                                            <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Visibility="{Binding IsUnitSelected, Converter={StaticResource BoolToVis}}" Fill="Aqua">
                                                <Polygon.RenderTransform>
                                                    <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                                </Polygon.RenderTransform>
                                            </Polygon>
                                            <!--NbUnité Joueur 1-->
                                            <Label Content="{Binding NbUniteJ1}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                                        </Grid>
                                        <Grid Visibility="{Binding HasUnitJ2, Converter={StaticResource BoolToVis}}">
                                            <!--Unité Joueur 2-->
                                            <Rectangle Width="20" Height="20" Fill="{Binding J2.peuple.Color, ElementName=_this}"/>
                                            <!-- Si une unité est sélectionnée -->
                                            <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Visibility="{Binding IsUnitSelected, Converter={StaticResource BoolToVis}}" Fill="Aqua">
                                                <Polygon.RenderTransform>
                                                    <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                                </Polygon.RenderTransform>
                                            </Polygon>
                                            <!--NbUnité Joueur 2-->
                                            <Label Content="{Binding NbUniteJ2}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                                        </Grid>
                                    </Grid>
                                    <!--ROW_IMPAIR-->
                                    <Grid Tag="{Binding Uid}" Height="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToHeightConverter}}" Visibility="{Binding RowPairReverse, Converter={StaticResource BoolToVis}}" MouseRightButtonUp="SelectUnit">
                                        <Grid.RenderTransform>
                                            <TranslateTransform X="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=40}"/>
                                        </Grid.RenderTransform>
                                        <!--Cases-->
                                        <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Stroke="Black">
                                            <Polygon.RenderTransform>
                                                <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                            </Polygon.RenderTransform>
                                            <Polygon.Fill>
                                                <ImageBrush ImageSource="{Binding Src}"/>
                                            </Polygon.Fill>
                                        </Polygon>
                                        <!--Unités-->
                                        <Grid Visibility="{Binding HasUnitJ1, Converter={StaticResource BoolToVis}}">
                                            <!--Unité Joueur 1-->
                                            <Rectangle Width="20" Height="20" Fill="{Binding J1.peuple.Color, ElementName=_this}"/>
                                            <!-- Si une unité est sélectionnée -->
                                            <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Visibility="{Binding IsUnitSelected, Converter={StaticResource BoolToVis}}" Fill="Aqua">
                                                <Polygon.RenderTransform>
                                                    <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                                </Polygon.RenderTransform>
                                            </Polygon>
                                            <!--NbUnité Joueur 1-->
                                            <Label Content="{Binding NbUniteJ1}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                                        </Grid>
                                        <Grid Visibility="{Binding HasUnitJ2, Converter={StaticResource BoolToVis}}">
                                            <!--Unité Joueur 2-->
                                            <Rectangle Width="20" Height="20" Fill="{Binding J2.peuple.Color, ElementName=_this}"/>
                                            <!-- Si une unité est sélectionnée -->
                                            <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Visibility="{Binding IsUnitSelected, Converter={StaticResource BoolToVis}}" Fill="Aqua">
                                                <Polygon.RenderTransform>
                                                    <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                                </Polygon.RenderTransform>
                                            </Polygon>
                                            <!--NbUnité Joueur 2-->
                                            <Label Content="{Binding NbUniteJ2}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                                        </Grid>
                                    </Grid>
                                    
                                    
                                    <!-- Box is selected -->
                                    <Grid Height="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToHeightConverter}}" Visibility="{Binding RowPair, Converter={StaticResource BoolToVis}}">
                                        <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Stroke="Red" Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}" StrokeThickness="2" Panel.ZIndex="2">
                                            <Polygon.RenderTransform>
                                                <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}"/>
                                            </Polygon.RenderTransform>
                                        </Polygon>
                                    </Grid>
                                    <Grid Height="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToHeightConverter}}" Visibility="{Binding RowPairReverse, Converter={StaticResource BoolToVis}}">
                                        <Polygon Points="{Binding map.Zoom, ElementName=_this, Mode=TwoWay, Converter={StaticResource RatioToPointsConverter}}" Stroke="Red" Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}" StrokeThickness="2" Panel.ZIndex="2">
                                            <Polygon.RenderTransform>
                                                <TranslateTransform Y="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=60}" X="{Binding map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=40}"/>
                                            </Polygon.RenderTransform>
                                        </Polygon>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
                <StackPanel Grid.Column="2">
                    <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" FontSize="15" Content="Tours et actions : "/>
                    <Slider DataContext="{Binding map, ElementName=_this}" Value="{Binding Zoom}" Minimum="0" Maximum="400" TickFrequency="10" TickPlacement="BottomRight" LargeChange="10" SmallChange="5"/>
                    <Label DataContext="{Binding map, ElementName=_this}" Content="{Binding Zoom}"/>
                    <!-- Informations case et unités sélectionnées -->
                    <Grid Visibility="{Binding map.IsBoxSelected, Converter={StaticResource BoolToVis}}">
                        <ListBox Name="items" DataContext="{Binding SelectedItem, ElementName=listCase}" ItemsSource="{Binding units}" SelectedItem="{Binding SelectedUnit}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Grid.Row="0" Visibility="{Binding unitsCount, Converter={StaticResource ListToVis}}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid VerticalAlignment="Center">
                                        <Image Source="res/flag.png" Visibility="{Binding HasAlreadyPlayed, Converter={StaticResource BoolToVis}}" Width="32" Height="32" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                        <Label Content="{Binding carac}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <ListBox DataContext="{Binding SelectedItem, ElementName=listCase}" ItemsSource="{Binding otherUnits}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Grid.Row="0" Visibility="{Binding otherUnitsCount, Converter={StaticResource ListToVis}}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid VerticalAlignment="Center">
                                        <Image Source="res/flag.png" Visibility="{Binding HasAlreadyPlayed, Converter={StaticResource BoolToVis}}" Width="32" Height="32" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                        <Label Content="{Binding carac}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </StackPanel>
            </Grid>
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" />
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" DataContext="{Binding J1, ElementName=_this}" Background="{Binding IsMyTurn, Converter={StaticResource BoolToBackgroundConverter}}">
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,0,0" FontSize="20" Content="{Binding joueur.nom}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,50,0,0" FontSize="15" Content="Nombre d'unités : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="185,50,0,0" FontSize="15" Content="{Binding peuple.nbUnite}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,80,0,0" FontSize="15" Content="Nombre de points : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="195,80,0,0" FontSize="15" Content="{Binding nbPoints}"/>
                    </Grid>
                    <Grid Grid.Column="1" DataContext="{Binding J2, ElementName=_this}" Background="{Binding IsMyTurn, Converter={StaticResource BoolToBackgroundConverter}}">
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,0,0" FontSize="20" Content="{Binding joueur.nom}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,50,0,0" FontSize="15" Content="Nombre d'unités : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="185,50,0,0" FontSize="15" Content="{Binding peuple.nbUnite}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,80,0,0" FontSize="15" Content="Nombre de points : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="195,80,0,0" FontSize="15" Content="{Binding nbPoints}"/>
                    </Grid>
                </Grid>
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="Nombre de tours : " VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
                        <Label Grid.Column="1" Content="{Binding nbTour}" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
                    </Grid>
                    <Button Grid.Row="1" Content="Fin tour" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20,0,20,0" Height="30" Click="NextTurn"/>
                    <Button Grid.Row="2" Content="Unité suivante" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20,0,20,0" Height="30"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Converters;assembly=Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="WarFareWPF.GameWindow"
        xmlns:local="clr-namespace:WarFareWPF"
        Title="GameWindow" Height="600" Width="800" x:Name="_this" DataContext="{Binding RelativeSource={RelativeSource Self}}" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" WindowState="Maximized" KeyUp="_this_KeyUp">
    <Border BorderThickness="2" BorderBrush="Black" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" IsEnabled="True" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ListBox Name="listCase" DataContext="{Binding game.map, ElementName=_this}" SelectedItem="{Binding SelectedBox, Mode=TwoWay}" ItemsSource="{Binding cases}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="{Binding Columns}" Margin="{Binding game.map.Zoom, ElementName=_this, Converter={StaticResource RatioToMarginConverter}, ConverterParameter=5 15 45 15}" VerticalAlignment="Center"  MouseWheel="Grid_MouseWheel"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <ContentPresenter Margin="{Binding game.map.Zoom, ElementName=_this, Converter={StaticResource RatioToMarginConverter}, ConverterParameter=0 -10 0 -10}"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Center">
                                    <!--ROW_PAIR-->
                                    <ContentControl Visibility="{Binding RowPair, Converter={StaticResource BoolToVis}}" Template="{DynamicResource ResourceKey=templateCase}"/>
                                    <!--ROW_IMPAIR-->
                                    <ContentControl Visibility="{Binding RowPairReverse, Converter={StaticResource BoolToVis}}" Template="{DynamicResource ResourceKey=templateCase}">
                                        <ContentControl.RenderTransform>
                                            <TranslateTransform X="{Binding game.map.Zoom, ElementName=_this, Converter={StaticResource RatioToCoordConverter}, ConverterParameter=40}"/>
                                        </ContentControl.RenderTransform>
                                    </ContentControl>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
                <StackPanel Grid.Column="2">
                    <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" FontSize="15" Content="Tours et actions : "/>
                    <Slider DataContext="{Binding game.map, ElementName=_this}" Value="{Binding Zoom}" Minimum="0" Maximum="400" TickFrequency="10" TickPlacement="BottomRight" LargeChange="10" SmallChange="5"/>
                    <Label DataContext="{Binding game.map, ElementName=_this}" Content="{Binding Zoom}"/>
                    <!-- Informations case et unités sélectionnées -->
                    <Grid Visibility="{Binding game.map.IsBoxSelected, Converter={StaticResource BoolToVis}}">
                        <ListBox Name="items" DataContext="{Binding SelectedItem, ElementName=listCase}" SelectedItem="{Binding SelectedUnit}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Grid.Row="0">
                            <ListBox.ItemsSource>
                                <MultiBinding Converter="{StaticResource PlayerToItemsSourceConverter}">
                                    <Binding ElementName="_this" Path="game.CurrentPlayer"/>
                                    <Binding Path="unitsJ1"/>
                                    <Binding Path="unitsJ2"/>
                                </MultiBinding>
                            </ListBox.ItemsSource>
                            <ListBox.Visibility>
                                <MultiBinding Converter="{StaticResource ListToVis}">
                                    <Binding ElementName="_this" Path="game.CurrentPlayer"/>
                                    <Binding Path="NbUniteJ1"/>
                                    <Binding Path="NbUniteJ2"/>
                                </MultiBinding>
                            </ListBox.Visibility>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="{x:Type ListBoxItem}">
                                    <Setter Property="Padding" Value="0,0,0,0"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl Template="{DynamicResource ResourceKey=templateInfo}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <ListBox DataContext="{Binding SelectedItem, ElementName=listCase}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Grid.Row="0">
                            <ListBox.ItemsSource>
                                <MultiBinding Converter="{StaticResource PlayerToItemsSourceConverter}">
                                    <Binding ElementName="_this" Path="game.OtherPlayer"/>
                                    <Binding Path="unitsJ1"/>
                                    <Binding Path="unitsJ2"/>
                                </MultiBinding>
                            </ListBox.ItemsSource>
                            <ListBox.Visibility>
                                <MultiBinding Converter="{StaticResource ListToVis}">
                                    <Binding ElementName="_this" Path="game.OtherPlayer"/>
                                    <Binding Path="NbUniteJ1"/>
                                    <Binding Path="NbUniteJ2"/>
                                </MultiBinding>
                            </ListBox.Visibility>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="{x:Type ListBoxItem}">
                                    <Setter Property="Padding" Value="0,0,0,0"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl Template="{DynamicResource ResourceKey=templateInfo}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </StackPanel>
            </Grid>
            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" />
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" DataContext="{Binding game.J1, ElementName=_this}" Background="{Binding IsMyTurn, Converter={StaticResource BoolToBackgroundConverter}}">
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,0,0" FontSize="20" Content="{Binding joueur.nom}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,50,0,0" FontSize="15" Content="Nombre d'unités : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="185,50,0,0" FontSize="15" Content="{Binding peuple.nbUnite}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,80,0,0" FontSize="15" Content="Nombre de points : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="195,80,0,0" FontSize="15" Content="{Binding nbPoints}"/>
                    </Grid>
                    <Grid Grid.Column="1" DataContext="{Binding game.J2, ElementName=_this}" Background="{Binding IsMyTurn, Converter={StaticResource BoolToBackgroundConverter}}">
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,0,0" FontSize="20" Content="{Binding joueur.nom}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,50,0,0" FontSize="15" Content="Nombre d'unités : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="185,50,0,0" FontSize="15" Content="{Binding peuple.nbUnite}"/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60,80,0,0" FontSize="15" Content="Nombre de points : "/>
                        <Label HorizontalAlignment="Left" VerticalAlignment="Top" Margin="195,80,0,0" FontSize="15" Content="{Binding nbPoints}"/>
                    </Grid>
                </Grid>
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="Nombre de tours : " VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
                        <Label Grid.Column="1" Content="{Binding game.nbTour}" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
                    </Grid>
                    <Button Grid.Row="1" Content="Fin tour" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20,0,20,0" Height="30" Click="NextTurn"/>
                    <Button Grid.Row="2" Content="Unité suivante" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20,0,20,0" Height="30"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>